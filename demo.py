class Solution:
    def is_rotation(self, A:list, B:list):
        for i in A:
            if len(B) != len (A)
                return False
            else:
                for i in A:
                    if i in B:
                        
        
